import React from "react";
// import PropTypes from "prop-types";

const Home = () => (
    <div>Lucas</div>
);

// Home.propTypes = {
//     test: PropTypes.string.isRequired,
// }

export default Home;
